// console.log( process.env.PORT );

const { SHELL, HOMEBREW_PREFIX } = process.env;

// console.log( SHELL, HOMEBREW_PREFIX );

export const character = [ 'Goku', 'Vegeta', 'Gohan', 'Trunks' ];

const [ , , , Trunks ] = character;

// console.log( Trunks );